# AGENTS.md

이 저장소를 Codex CLI/Gemini CLI 등 Claude Code 이외의 에이전트로 다룰 때 자동으로 읽히는 진입점이다.

**실제 작업 지침은 전부 [`CLAUDE.md`](./CLAUDE.md)에 있다 — 이 파일을 별도로 두지 말고 반드시 `CLAUDE.md`를 전체 읽어라.**

이 파일이 `CLAUDE.md`와 내용을 복제하지 않는 이유: 예전에는 두 파일이 동일한 내용을 각자 담고 있었는데, 한쪽만 갱신되고 다른 쪽이 낡은 채로 남는 일이 반복됐다(예: 마이그레이션 적용 여부, R1 진행 단계 서술이 서로 달라짐). 진짜 내용을 `CLAUDE.md` 한 곳에만 두고 이 파일은 안내판 역할만 하도록 바꿔, 어떤 도구로 열든 항상 같은 최신 지침을 보게 했다.
