# 독서토론 앱 — Claude Code 프롬프트 v2

v1에서 바뀐 것
- **논제 2번(발췌 나눔)이 2단 중첩 구조임을 발견** → 코멘트를 v2에서 MVP로 끌어올림
- **데스크톱 우선으로 전환** → 매트릭스 뷰(한눈에 보기) 추가
- 회차 템플릿 기능 추가 (관리자 반복 노동 제거)
- 참여자 별칭(선/선희, 희/희진) 지원
- 책 선정자·논제 담당자 개념 추가
- 이전 앱이 사장된 원인에 대한 대응 장치 추가

확정 사항
- 인증: 공통 암호 + 이름 선택 (개인 PIN 4자리는 스위치로 준비)
- DB: Supabase (Postgres만 사용, Supabase Auth 미사용)
- 배포: Vercel Hobby
- 스택: Next.js App Router + TypeScript + Tailwind
- 주 사용 환경: **데스크톱**, 보조로 모바일

---

## A. 원본 문서에서 읽어낸 실제 구조

```
『허삼관매혈기』

■ 자유논제 1. <책을 소개하는 한 문장 요약. 책을 선정하신 희진님께 부탁드립니다.>
   『 』을 어떻게 보셨나요? 별점과 소감을 나눠주세요.
   별점 ☆ ☆ ☆ ☆ ☆
   소감
   희진:
   윤선:
   수열:
   선희:
   용훈:

2. 인상 깊게 읽은 부분이나 발췌가 있다면 이유와 함께 소개해 주세요.
   다른 분들은 거기에 자신의 사유를 더해 봅니다.

   희진 발췌
   이유

   사유 더하기
   용훈:
   수열:
   선:
   희:
   혜:
   윤선:

   수열 발췌
   이유
   ...
```

여기서 확정되는 사실
1. 논제 유형이 최소 두 종류다. 평평한 것(1번)과 **중첩된 것(2번)**.
2. 2번은 각자가 발췌+이유를 올리고, **그 아래 다른 사람들이 사유를 덧붙인다.**
3. 별점은 논제 1에 붙어 있지만 실질은 회차 단위 평가다.
4. 책 선정자가 따로 있고, 그 사람이 첫 논제를 맡는다.
5. 이름을 축약해 쓴다(선/선희, 희/희진). 별칭 처리가 필요하다.
6. 논제 번호가 `■ 자유논제 1.` 다음에 그냥 `2.`로 이어진다. 파서가 둘 다 인식해야 한다.

---

## B. 이전 앱이 사장된 이유에 대한 대응

독서모임 앱이 죽는 경로는 대개 정해져 있다. 아래 장치를 처음부터 넣는다.

| 죽는 경로 | 대응 장치 | 반영 위치 |
|---|---|---|
| 링크가 단톡방 위로 사라짐 | 공유 문구 자동 생성 + 클립보드 복사 | Phase 4 |
| 열었는데 아무도 안 씀 → 재방문 안 함 | 참여 현황 표시("5명 중 3명 작성") | Phase 4 |
| 관리자가 매달 논제 세팅에 지침 | 회차 템플릿에서 복제 생성 | Phase 3 |
| 과거 자료는 구글 드라이브에 → 두 곳을 오감 | 붙여넣기 일괄 이관 | Phase 3 |
| 로그인이 매번 귀찮음 | 세션 90일 유지 | Phase 2 |
| 작성 단계가 많음 | 회차 상세에서 인라인 편집(별도 페이지 이동 없음) | Phase 4 |

핵심은 **앱이 알림을 못 보내니 사람이 보내게 만드는 것**이다.
관리자가 버튼 하나로 아래 문구를 복사해 단톡방에 붙여넣는다.

```
📖 『허삼관매혈기』 논제가 올라왔습니다
모임: 3월 15일 (토) 오후 2시
작성: 3/5명 완료 · 마감 3월 14일
https://.../s/12
```

---

## C. 데이터 모델

```
books
  id, title, author, cover_url, memo

members
  id, name, aliases text[], role ('member'|'admin'), is_active, pin_hash

sessions
  id, book_id, meets_at, deadline_at,
  selector_member_id   -- 책 선정자
  host_member_id       -- 진행자
  status ('draft'|'open'|'closed')
  blind_enabled        -- v2용, 지금은 false 고정

topics
  id, session_id, order_no,
  kind ('free'|'excerpt'|'choice')   -- ★ 핵심
  title, body,
  assigned_member_id,   -- 담당자 (요약은 선정자 등)
  has_rating bool       -- 별점 위젯 표시 여부

answers                 -- 논제에 대한 1인 1답변
  id, topic_id, member_id,
  body,                 -- free: 소감 / choice: 근거
  excerpt_text,         -- excerpt 전용: 발췌 원문
  excerpt_reason,       -- excerpt 전용: 고른 이유
  submitted_at, updated_at

replies                 -- ★ "사유 더하기". answers에 달린다. MVP 필수
  id, answer_id, member_id, body, created_at

ratings
  id, session_id, member_id, stars (1~5)

votes                   -- v2용 (choice 논제)
  id, topic_id, member_id, choice

login_attempts
  id, ip, attempted_at, success
```

`kind`에 따른 UI 분기

| kind | 화면 |
|---|---|
| `free` | 논제 아래 참여자 답변 카드 나열. 평평함. |
| `excerpt` | 참여자별 발췌 블록(인용 서식) + 이유 → 그 아래 사유 더하기 스레드 |
| `choice` | 찬반 집계 막대 + 진영별 근거 (v2) |

MVP는 `free`와 `excerpt` 둘 다 구현한다. `choice`는 스키마만 둔다.

---

## Phase 0 — 스펙 문서 (코드 금지)

```
너는 이 프로젝트의 아키텍트다. 이번 턴에는 코드를 한 줄도 작성하지 마라.
docs/ 폴더의 명세 문서만 작성한다.

## 프로젝트
독서모임 온라인 토론 기록 앱.
현재 구글 문서로 운영 중인 월 1회 독서토론을 전용 웹앱으로 옮긴다.
이전에 유사한 앱을 만들었으나 사용되지 않고 사장된 이력이 있다.
따라서 "쓰이게 만드는 장치"를 기능만큼 중요하게 다룬다.

## 이용자
- 참여자 5~8명 (고정 멤버, 40~50대)
- 관리자 1명
- 주 사용 환경은 데스크톱. 모바일은 보조.
- 사용 빈도: 월 1회 집중. 나머지 기간은 유휴.

## 현행 구글 문서의 실제 구조 (반드시 계승할 것)
문서 1개 = 책 1권 = 모임 1회차. 안에 논제가 번호로 나열된다.
논제는 두 종류다.

[유형 A — 평평한 논제]
■ 자유논제 1. <책을 소개하는 한 문장 요약. 책을 선정하신 희진님께 부탁드립니다.>
   별점 ☆ ☆ ☆ ☆ ☆
   소감
   희진:
   윤선:
   수열:

[유형 B — 중첩 논제 (발췌 나눔)]
2. 인상 깊게 읽은 부분이나 발췌가 있다면 이유와 함께 소개해 주세요.
   다른 분들은 거기에 자신의 사유를 더해 봅니다.

   희진 발췌
   이유

   사유 더하기
   용훈:
   수열:
   선:

   수열 발췌
   이유
   ...

유형 B가 핵심이다. 각 참여자가 발췌와 이유를 올리고,
그 발췌 아래에 다른 참여자들이 각자의 사유를 덧붙인다.
즉 답변에 답변이 달리는 2단 구조다.

추가 사실
- 참여자들은 이름을 축약해 쓴다 (선희→선, 희진→희). 별칭 매칭이 필요하다.
- 회차마다 "책 선정자"가 있고 그 사람이 첫 논제를 맡는다.
- 논제 번호가 "■ 자유논제 1." 다음 "2."로 이어진다.

## 해결해야 할 문제 (앱의 존재 이유)
1. 선형 스크롤 — 같은 논제에 대한 여러 답변을 나란히 비교할 수 없다
2. 중첩 구조가 들여쓰기로만 표현되어 누가 누구 발췌에 답했는지 흐리다
3. 편집 사고 — 남의 글을 실수로 수정하거나 지운다
4. 아카이브 부재 — 지난 회차와 지난 책을 다시 찾을 수 없다
5. 관리자가 매달 같은 뼈대를 손으로 다시 만든다

## 1차 배포(MVP) 범위
- 로그인 (공통 암호 + 이름 선택)
- 회차 목록 / 회차 상세
- 논제 유형 free(평평) + excerpt(발췌·사유더하기) 작성과 조회
- 세 가지 뷰: 논제별 / 사람별 / 한눈에(매트릭스)
- 관리자: 책·회차·논제·참여자 CRUD, 회차 템플릿 복제, 붙여넣기 일괄 이관
- 공유 문구 생성, 참여 현황 표시

## v2 범위 (지금 만들지 않지만 스키마에는 미리 반영)
- 블라인드 제출 (본인 제출 전 타인 답변 잠금)
- 선택논제(choice) 찬반 투표
- 마크다운 내보내기
- 전문 검색

중요: MVP는 화면만 줄이고 스키마는 v2까지 담아둔다.

## 기술 제약
- 배포: Vercel Hobby(무료). 유료 기능 의존 금지
- DB: Supabase Postgres 무료 플랜
- 스택: Next.js App Router + TypeScript + Tailwind
- 인증: Supabase Auth를 쓰지 않는다. 자체 세션 쿠키 방식이다.

## 보안 원칙 (문서에 명시적으로 기록할 것)
Supabase Auth를 쓰지 않으므로 auth.uid() 기반 RLS를 쓸 수 없다. 따라서:
1. 모든 테이블 RLS 활성화, 정책은 만들지 않는다 (전면 거부)
2. 브라우저에서 Supabase를 직접 호출하지 않는다
3. 모든 DB 접근은 Next.js 서버에서만 한다
4. SUPABASE_SERVICE_ROLE_KEY에 NEXT_PUBLIC_ 접두사를 붙이지 않는다
5. 권한 검사는 서버에서 한다. 버튼을 숨기는 것은 보안이 아니다

## 유지보수 제약
- 인증 로직은 lib/auth.ts 한 파일에만 둔다
- 이후 유지보수를 Codex CLI / Gemini CLI로도 수행한다
  마법 같은 추상화를 피하고 폴더 구조와 명명 규칙을 명시적으로 만들어라

## 이번 턴 산출물
1. docs/SPEC.md — 화면 목록, 기능, 사용자 흐름, 상태 전이
2. docs/SCHEMA.md — 테이블 정의, 관계도, 시드 예시
   특히 topics.kind에 따른 answers/replies 사용 방식을 명확히 기술할 것
3. docs/DECISIONS.md — 결정과 이유, 채택하지 않은 대안
4. docs/SECURITY.md — 권한 매트릭스, 서버 측 필수 검증 체크리스트
5. docs/RETENTION.md — 이 앱이 계속 쓰이게 만드는 장치들과 그 근거
6. docs/OPEN_QUESTIONS.md — 임의 판단하지 말고 질문으로 모을 것
7. CLAUDE.md / AGENTS.md — 동일 내용

## 마지막
문서를 다 쓴 뒤, 내가 놓쳤다고 판단되는 설계 리스크 3가지를 짚어라.
특히 "이전 앱이 왜 안 쓰였을까"에 대한 너의 가설을 함께 제시하라.
```

---

## Phase 1 — 스캐폴딩 + 스키마 + 시드

```
docs/SPEC.md와 docs/SCHEMA.md를 읽고 시작하라.

## 이번 턴 목표
프로젝트 뼈대, DB 스키마, 시드 데이터. 화면은 만들지 않는다.

## 작업
1. Next.js(App Router) + TypeScript + Tailwind 프로젝트 생성
2. lib/supabase/server.ts 하나만 만든다
   - 파일 최상단에 import 'server-only'
   - SUPABASE_SERVICE_ROLE_KEY 사용
   - 클라이언트용 Supabase 클라이언트는 만들지 않는다
3. supabase/migrations/ 에 SQL 마이그레이션
   - 전 테이블 ENABLE ROW LEVEL SECURITY, 정책 없음
   - answers와 replies의 관계, topics.kind 제약(CHECK)을 정확히 반영
4. supabase/seed.sql
   - 참여자 6명: 수열, 윤선, 희진, 선희, 용훈, 혜정
     aliases에 축약형을 넣을 것 (선희→'선', 희진→'희', 혜정→'혜')
   - 책 2권: 『독학력』(완료 회차), 『허삼관매혈기』(진행 중 회차)
   - 『독학력』 회차: free 논제 2개 + excerpt 논제 1개, 답변과 사유더하기 채워넣기
   - 『허삼관매혈기』 회차: 논제만 있고 답변은 비어 있음 (실제 상황 재현)
5. .env.example (실제 키 금지)
6. app/_debug/page.tsx — 서버 컴포넌트에서 조회해 JSON 출력
   excerpt 논제의 answers와 replies가 중첩되어 나오는지 확인 가능해야 한다
   Phase 4에서 삭제 예정임을 주석으로 남길 것

## 하지 말 것
- 로그인, 관리자, 참여자 화면, 디자인
- 스키마 임의 변경 (변경 필요 시 먼저 이유를 설명하고 승인받을 것)

## 끝나면
마이그레이션 적용과 시드 주입 명령을 순서대로 알려주고,
.env에 넣을 값이 Supabase 대시보드 어디에 있는지 알려줘라.
```

**종료 조건**: `/_debug`에서 발췌 논제의 `answers → replies` 중첩이 눈으로 확인된다.

---

## Phase 2 — 인증 + 라우트 보호

```
docs/SECURITY.md를 읽고 시작하라.

## 인증 방식
2단계. 공통 암호 입력(외부 차단) → 명단에서 이름 선택(신원 지정).

## 구현 요구사항
- 공통 암호는 환경변수 SITE_PASSWORD_HASH에 bcrypt 해시로 둔다
  평문을 코드나 환경변수에 두지 않는다
  scripts/hash-password.ts 를 만들어 해시를 생성할 수 있게 한다
- 세션은 서명된 JWT 쿠키 (jose)
  - httpOnly, secure, sameSite 'lax', maxAge 90일
  - 90일인 이유: 월 1회 사용이므로 30일이면 매번 다시 로그인하게 된다
  - payload에는 memberId와 role만
- 인증 함수는 전부 lib/auth.ts 에 둔다
  외부에는 getSession(), requireSession(), requireAdmin() 셋만 노출한다
  이 시그니처는 인증 방식을 교체해도 유지되도록 설계하라
- middleware.ts로 /login 과 /api/auth/* 외 전 경로 보호
- 관리자 판별은 members.role = 'admin'
  미들웨어와 각 서버 액션 양쪽에서 검사한다

## 무차별 대입 방지
login_attempts 테이블로 같은 IP에서 10분 내 5회 실패 시 10분 차단.
차단 메시지는 "잠시 후 다시 시도해 주세요"로만 표시한다.

## 개인 PIN (환경변수 REQUIRE_MEMBER_PIN, 기본 false)
true면 이름 선택 후 4자리 PIN을 추가로 받는다 (members.pin_hash, bcrypt).
지금은 false로 두되 양쪽 다 동작하게 작성하라.

## 화면
- /login 한 페이지 안에서 단계 전환
- 데스크톱 기준으로 화면 중앙 카드, 최대 폭 420px
- 이름 선택은 큰 버튼 그리드 (드롭다운 금지)
- 헤더 우측에 현재 로그인한 이름과 로그아웃

## 하지 말 것
- Supabase Auth 사용
- 클라이언트 측 권한 검사만으로 끝내는 것
```

**종료 조건**: 비로그인 상태로 `/` 접속 시 튕긴다. 일반 계정으로 `/admin` 접근이 막힌다.

---

## Phase 3 — 관리자 + 템플릿 + 이관

```
## 이번 턴 목표
관리자가 매달 회차를 세팅하는 노동을 최소화한다.
관리자가 지치면 앱이 죽는다. 이 Phase의 목적은 그것을 막는 것이다.

## 화면 (/admin 하위)
1. /admin/members
   - 이름, 별칭 목록(쉼표 구분 입력), 역할, 활성 여부
   - 삭제 대신 is_active = false
2. /admin/books — 제목, 저자, 표지 URL, 메모
3. /admin/sessions
   - 책, 모임일, 마감일, 책 선정자, 진행자, 상태
4. /admin/sessions/[id]/topics
   - 논제 추가/수정/삭제, 순번 숫자 입력으로 정렬
   - 유형 선택: free / excerpt / choice
   - 담당자 지정(선택), 별점 표시 여부 체크박스
   - choice 유형은 목록에 나타나되 전용 편집 UI는 만들지 않는다

## 회차 템플릿 (중요)
매달 논제 1번(요약+별점)과 2번(발췌 나눔)은 사실상 고정이다.
- /admin/templates 에서 논제 묶음을 템플릿으로 저장
- 새 회차 생성 시 "템플릿에서 시작" 선택 가능
- 기본 템플릿 하나를 시드에 넣어둔다
  1) free · "책을 소개하는 한 문장 요약" · 담당=책 선정자 · 별점 있음
  2) excerpt · "인상 깊게 읽은 부분이나 발췌를 이유와 함께 소개해 주세요"
- 이전 회차를 골라 논제 구조만 복제하는 기능도 넣는다

## 붙여넣기 일괄 이관
/admin/sessions/[id]/import
구글 문서에서 복사한 텍스트를 붙여넣으면 파싱한다.

파싱 규칙:
- "■"로 시작하거나 "숫자." 로 시작하는 줄 = 새 논제
- 논제 본문에 "발췌", "사유 더하기"가 포함되면 kind = 'excerpt'로 판정
- "별점" 줄이 있으면 has_rating = true
- excerpt 논제 안에서:
  - "이름 발췌" 형태의 줄 = 그 참여자의 answer 시작
  - 그 다음 "이유" 라벨 이후 = excerpt_reason
  - "사유 더하기" 라벨 이후의 "이름:" 줄들 = 그 answer에 달린 replies
- free 논제 안에서:
  - "이름:" 줄 = 그 참여자의 answer
- 이름은 members.name과 aliases 양쪽에서 찾는다
- 매칭 실패한 이름은 목록으로 보여주고 연결할지 새로 만들지 물어본다

반드시 미리보기를 먼저 보여준다. 확인 버튼을 눌러야 저장된다.
미리보기에는 "논제 3개, 답변 15개, 사유더하기 22개를 만듭니다" 같은 요약을 표시한다.

## 원칙
- 모든 쓰기 작업은 Server Action이며 첫 줄에서 requireAdmin() 호출
- 답변이 달린 논제 삭제 시도는 경고 후 차단
- 삭제는 확인 모달을 거친다
```

**종료 조건**: 『허삼관매혈기』 회차를 템플릿에서 30초 안에 만들 수 있고, 『독학력』 완료 문서를 붙여넣어 이관할 수 있다.

---

## Phase 4 — 참여자 화면 (세 가지 뷰)

```
## 이번 턴 목표
이 앱의 존재 이유인 화면. 여기가 제일 중요하다.
주 사용 환경은 데스크톱이다. 넓은 화면을 적극적으로 쓴다.

## 화면 1 — / (회차 목록)
- 진행 중 회차를 상단에 큰 카드로
  책 표지, 제목, 모임일까지 남은 일수, "5명 중 3명 작성" 참여 현황
  내 상태를 명확히: "내 답변 2/4 작성"
- 지난 회차는 아래에 연도별 그룹으로 목록

## 화면 2 — /s/[id] (회차 상세)
URL을 짧게 유지한다 (/sessions/ 가 아니라 /s/).
상단에 뷰 전환 탭 세 개. 상태는 URL 쿼리(?view=)에 넣는다.

### [논제별] (기본, ?view=topic)
- 좌측에 논제 목록 사이드바 고정 (데스크톱 1024px 이상)
  각 논제에 내 작성 여부를 점으로 표시
- 우측 본문에 선택된 논제
- kind = 'free' 인 경우
  - 논제 본문 → 별점 위젯(has_rating일 때) → 참여자 답변 카드
  - 데스크톱 2열 그리드(1440px 이상은 3열), 모바일 1열
  - 내 카드는 항상 첫 자리, 테두리 강조, 클릭 시 인라인 편집
- kind = 'excerpt' 인 경우
  - 참여자별 블록을 세로로 쌓는다
  - 각 블록: 발췌문(좌측 세로선 + 세리프 계열 인용 서식)
    → 이유 → 구분선 → "사유 더하기" 스레드(들여쓰기)
  - 내 발췌 블록이 없으면 상단에 "내 발췌 추가하기" 버튼
  - 남의 발췌 블록 하단에는 항상 "사유 더하기" 입력창(접힌 상태)
  - 사유 더하기는 짧은 글이므로 2~3줄 입력창이면 충분하다

### [사람별] (?view=member)
- 상단 참여자 탭
- 선택한 사람의 전 논제 답변 + 그 사람이 단 사유더하기를 세로로
- 읽기 전용

### [한눈에] (?view=matrix) ★ 데스크톱 전용
- 행 = 참여자, 열 = 논제인 표
- 셀에는 작성 여부 아이콘과 첫 문장 20자
- 셀 클릭 시 논제별 보기의 해당 위치로 이동
- 진행자가 토론 직전에 전체 판을 파악하는 용도다
- 1024px 미만에서는 이 탭을 숨긴다

## 화면 3 — /me
내가 쓴 글 모아보기. 회차 역순. 답변과 사유더하기를 모두 포함.

## 공유 문구 생성 (회차 상세 우상단)
버튼을 누르면 아래 형식 문구를 클립보드에 복사하고 토스트를 띄운다.

📖 『{책 제목}』 논제가 올라왔습니다
모임: {모임일} {요일} 
작성: {n}/{전체}명 완료 · 마감 {마감일}
{절대 URL}

관리자뿐 아니라 모든 참여자가 쓸 수 있게 한다.

## 편집 동작
- 자동 저장하지 않는다. 저장 버튼을 명시한다
- 저장 후 "저장됨 · 방금"을 3초간 표시
- 작성 중 이탈 시 확인창
- 리치 에디터를 쓰지 않는다. textarea + whitespace-pre-wrap
- 발췌문 입력만 예외적으로 별도 필드로 분리한다

## 디자인 지침
- 본문 Pretendard, 16~17px, 행간 1.7
- 발췌문은 세리프 계열로 대비를 준다
- 본문 컬럼 최대 폭 68자 내외. 화면이 넓어도 한 줄을 늘리지 않는다
- 강조색 1개 + 회색 계열. 이전 앱의 짙은 청회색(#1e293b) 계열을 이어가되
  본문 배경은 밝게 유지한다 (긴 글을 오래 읽는 화면이다)
- 다크 모드는 만들지 않는다

## 마지막
app/_debug/ 를 삭제하라.
```

**종료 조건**: 두 계정으로 동시에 접속해 한 사람이 발췌를 올리고 다른 사람이 사유를 더한 뒤, 세 뷰 모두에서 제대로 보이는지 확인한다.

---

## Phase 5 — 배포 + 일시정지 방지 + 점검

```
## 1. keep-alive (필수)
Supabase 무료 플랜은 7일 무활동 시 프로젝트를 일시정지한다.
이 앱은 월 1회만 쓰므로 대응하지 않으면 매달 꺼져 있다.

- app/api/keep-alive/route.ts
  - 가벼운 SELECT 1 후 200 반환
  - Authorization 헤더의 Bearer 토큰이 CRON_SECRET과 다르면 401
- vercel.json의 crons에 schedule "0 3 * * *" (매일 1회)
  Vercel Hobby는 하루 1회까지만 허용한다.
  하루 2회 이상 표현식은 배포 자체가 실패하므로 쓰지 마라.

## 2. 배포 준비
- .env.example 최신화, Vercel 등록 환경변수 목록을 표로 정리
- README.md에 로컬 실행법과 배포 절차

## 3. 자체 점검 (배포 전) — 결과를 표로 보고할 것
- [ ] 빌드 산출물(.next)에 SERVICE_ROLE_KEY 문자열이 없는가
      → grep으로 실제 확인하고 결과를 보여줄 것
- [ ] 클라이언트 컴포넌트에서 supabase-js를 import하는 곳이 없는가
- [ ] 모든 Server Action 첫 줄에 requireSession()/requireAdmin()이 있는가
      → 파일 목록과 함께 제시
- [ ] 남의 answer id로 수정 요청을 보내면 서버가 거부하는가
      → 남의 reply 삭제 시도도 함께 확인
- [ ] 전 테이블 RLS가 켜져 있는가
- [ ] middleware matcher에서 빠진 경로가 없는가

## 4. 마지막
docs/DECISIONS.md에 Phase 1~5에서 실제로 바뀐 결정을 추가 기록하라.
```

---

## Codex / Gemini CLI 인계 프롬프트

```
이 저장소는 6~8명 사적 독서모임의 토론 기록 웹앱이다.
먼저 AGENTS.md, docs/SPEC.md, docs/SECURITY.md 를 읽어라.

이번 작업은 리뷰다. 코드를 수정하지 말고 보고서만 작성하라.

## 배경
- Supabase Auth를 쓰지 않고 공통 암호 + 세션 쿠키로 인증한다
- RLS는 전면 거부 상태이고, 모든 DB 접근은 서버에서 service_role 키로 한다
- 이 구조에서는 서버 측 권한 검사가 유일한 방어선이다

## 점검 항목
1. service_role 키가 클라이언트 번들로 유출될 import 경로가 있는가
2. 권한 검사가 누락된 Server Action 또는 Route Handler가 있는가
3. 특히 replies(사유 더하기) 생성·수정·삭제에서
   작성자 본인 확인이 서버에서 이뤄지는가
4. 사용자 입력이 SQL이나 렌더링에 안전하지 않게 들어가는 곳이 있는가
5. 세션 쿠키의 서명 검증을 우회할 지점이 있는가
6. 로그인 시도 제한이 서버리스 환경에서 실제로 동작하는가
7. 붙여넣기 이관 파서가 악의적 입력에 대해 안전한가

## 보고 형식
[심각/보통/낮음] 등급, 파일 경로와 줄 번호, 재현 방법, 수정 방향.
수정 코드는 내가 승인한 뒤에 작성한다.
```

---

## 환경변수

| 이름 | 용도 | 클라이언트 노출 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL | 가능 |
| `NEXT_PUBLIC_SITE_URL` | 공유 문구용 절대 URL | 가능 |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 전용 DB 접근 | **절대 금지** |
| `SITE_PASSWORD_HASH` | 공통 암호 bcrypt 해시 | 금지 |
| `SESSION_SECRET` | JWT 서명 키 (32자 이상) | 금지 |
| `REQUIRE_MEMBER_PIN` | 개인 PIN 사용 여부 (기본 false) | 금지 |
| `CRON_SECRET` | keep-alive 인증 | 금지 |

## 체크포인트

| Phase | 확인 |
|---|---|
| 0 | `OPEN_QUESTIONS.md`를 읽고 답을 정했는가 |
| 1 | `/_debug`에서 발췌 논제의 answers→replies 중첩이 보이는가 |
| 2 | 비로그인 튕김 / 일반 계정의 `/admin` 차단 |
| 3 | 템플릿으로 회차 30초 생성 / 구글 문서 이관 성공 |
| 4 | 두 계정으로 발췌+사유더하기 왕복 / 세 뷰 모두 정상 |
| 5 | 빌드에 service_role 키 없음 / cron 등록됨 |
